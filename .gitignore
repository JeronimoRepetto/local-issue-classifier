node_modules
dist
coverage
*.log
.DS_Store

# editor junk
.idea/
*.swp
*.swo

# secrets — keep out of the repo (only .env.example is versioned)
.env
.env.*
!.env.example

# CodeGraph local index (machine-local, per worktree)
.codegraph/

# Wrangler's local cache/state (e.g. from `wrangler pages dev`)
.wrangler/

# pnpm local:kev's Kev checkout (FB-4, scripts/local-kev.mjs)
.local/

# In-browser model files (docs/browser-inference.md): the browser keeps them in its
# Cache API; a Node run of transformers.js or a phase-B export would write here.
.cache/
models/
*.onnx
*.onnx_data
